scripted_gui_triggers = {
    ShowInheritanceLensPanel = {
        trigger = { has_character_flag = inheritance_lens_panel_open }
    }

    ShowInheritanceLensLegend = {
        trigger = { has_character_flag = inheritance_lens_map_mode_active }
    }

    ShowInheritanceLensSettings = {
        trigger = { has_character_flag = inheritance_lens_settings_open }
    }

    IsPlayer = {
        trigger = { is_player = yes }
    }
}
